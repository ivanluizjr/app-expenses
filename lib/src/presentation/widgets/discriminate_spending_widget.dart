import 'package:flutter/material.dart';

class DiscriminateSpending extends StatelessWidget {
  const DiscriminateSpending ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
